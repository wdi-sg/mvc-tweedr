CREATE TABLE IF NOT EXISTS users (id SERIAL PRIMARY KEY, username TEXT NOT NULL, password TEXT NOT NULL);

CREATE TABLE IF NOT EXISTS tweeds (id SERIAL PRIMARY KEY, tweed TEXT, user_id INTEGER REFERENCES users (id));